//draw_arc.dart line 98
// else if (circleName == Constants.minusCircleName) {
// paint = Paint()..color = Constants.deactiveFunctionColor;
//
// canvas.drawCircle(
// Offset(size.width / 2 + 55, size.height / 2), 25, paint);
// } else if (circleName == Constants.multiplyCircleName) {
// paint = Paint()..color = Constants.deactiveFunctionColor;
//
// canvas.drawCircle(
// Offset(size.width / 2 + 30, size.height / 2 + 80), 25, paint);
// } else if (circleName == Constants.dividerCircleName) {
// paint = Paint()..color = Constants.deactiveFunctionColor;
//
// canvas.drawCircle(
// Offset(size.width / 2 - 20, size.height / 2 + 150), 25, paint);
// } else if (circleName == Constants.simpleLineName) {
// paint = Paint()..color = Constants.deactiveFunctionColor;
//
// canvas.drawCircle(
// Offset(size.width / 2 - 20, size.height / 2 + 150), 25, paint);
// }

//line_painter.dart line 17
// print("Line Size : $size");
// double diameter = Constants.whiteCircleDiameter;
// double startPointdx = diameter * 1 / 6;
// double endPointdx = diameter * 5 / 6 - 135;
// print("diameter diameter * 1 / 6 (x start) : ${diameter * 1 / 6}");
// print("diameter diameter * 1 / 2 (y start) : ${diameter * 1 / 2}");
// print("diameter diameter * 5 / 6 (x end) : ${diameter * 5 / 6}");
// print("diameter diameter * 1 / 2 (y end) : ${diameter * 1 / 2}");

//draw_arc.dart after line 143
// else if (circleName == Constants.plusCircleName) {
//   paint = Paint()..color = Constants.deactiveFunctionColor;
//
//   // canvas.drawCircle(
//   //     Offset(size.width / 2 + 40, size.height / 2 - 85), 25, paint);
//
//   canvas.drawCircle(
//       Offset(size.width / 2 + 40, size.height / 2 - 85), 45, paint);
// }
